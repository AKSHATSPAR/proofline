# Required only when processing new PDFs. Never commit the real key.
OPENAI_API_KEY=

# A smaller structured-output model keeps the prototype inexpensive.
OPENAI_MODEL=gpt-5.4-mini

# Optional. Defaults to data/proofline.db.
PROOFLINE_DB_PATH=data/proofline.db

